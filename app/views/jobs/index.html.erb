
<div class="hero-block">
  <div class="hero-inner">
    <h1 class="hero-title"> 66K 工作網 </h1>

    <div class="hero-desc">
      <p class="lead">
        <%= link_to("66K 工作網", "/") %> 是一個提供 66K+ 新台幣工作機會的求職網站。
      </p>
      <p class="lead">
        我們希望藉由這個網站，能讓大家找到且應徵上高薪且優質的工作。 憑藉自己的努力和天份也會變 Winner。
      </p>
    </div>
  </div>
</div>

<%= render :partial => "jobs/search_bar" %>


<% @categories.each do |category| %>

<%= render :partial => "category_block", :locals => { :category => category } %>
<% end %>